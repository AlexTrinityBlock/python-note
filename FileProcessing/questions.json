[
  {
    "type": "single",
    "question": "Part 1 of 4: How do you import Python's SQLite interface?\n\nThis is a progressive question set: each part builds on the same program, and later parts reveal the answers to earlier parts. Answer the parts in order.\n\n```python\n# Write the first line of the program:\n```",
    "options": [
      "`import SQLite3`",
      "`import sqlite3`",
      "`from sqlite import sqlite3`",
      "`import sqlite`"
    ],
    "answer": [
      1
    ],
    "explanation": "Incorrect - the correct answer is B: `import sqlite3`.\n\nExplanation\n\nPython's standard library provides its SQLite DB-API interface in the module named `sqlite3`, written entirely in lowercase. The first line of the program is:\n\n```python\nimport sqlite3\n```\n\nThis import binds the module object to the name `sqlite3` in the current namespace. The next part can therefore call `sqlite3.connect(...)`.\n\nStep by step:\n\n1. Python locates the standard-library module named `sqlite3`.\n2. It loads the module if it has not already been loaded.\n3. It binds the module to the name `sqlite3`.\n4. Later code can use names such as `sqlite3.connect` and `sqlite3.Connection`.\n\nWhy the other options fail:\n\n| Option | Code | Result |\n|--------|------|--------|\n| B | `import sqlite3` | Correct: imports Python's SQLite interface. |\n| A | `import SQLite3` | Fails because module names are case-sensitive and the standard name is lowercase. |\n| C | `from sqlite import sqlite3` | Fails because the standard-library module is `sqlite3`, not a package named `sqlite` containing `sqlite3`. |\n| D | `import sqlite` | Does not import the Python SQLite interface; the module name is `sqlite3`. |\n\nThe completed program after Part 1 is simply:\n\n```python\nimport sqlite3\n```"
  },
  {
    "type": "single",
    "question": "Part 2 of 4: Which line opens the database file `hello.db` and stores the connection?\n\n```python\nimport sqlite3\n\n# Write the next line of the program:\n```",
    "options": [
      "`conn = sqlite3.open('hello.db')`",
      "`conn = connect.sqlite3('hello.db')`",
      "`conn = sqlite3.connect_db('hello.db')`",
      "`conn = sqlite3.connect('hello.db')`"
    ],
    "answer": [
      3
    ],
    "explanation": "Incorrect - the correct answer is D: `conn = sqlite3.connect('hello.db')`.\n\nExplanation\n\n`sqlite3.connect(database)` opens a connection to the database identified by its argument and returns a `sqlite3.Connection` object. With the relative path `'hello.db'`, SQLite uses a database file named `hello.db` in the process's current working directory, creating it if it does not already exist.\n\nThe complete program so far is:\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('hello.db')\n```\n\nStep by step:\n\n1. `import sqlite3` makes the module available under the name `sqlite3`.\n2. Python evaluates the expression `sqlite3.connect('hello.db')`.\n3. `connect()` opens the existing database file, or creates the file when it does not exist.\n4. `connect()` returns a `sqlite3.Connection` object.\n5. The assignment binds that object to `conn`.\n\nWhy the other options fail:\n\n| Option | Code | Result |\n|--------|------|--------|\n| D | `conn = sqlite3.connect('hello.db')` | Correct: calls the module's `connect()` function and stores its returned connection. |\n| A | `conn = sqlite3.open('hello.db')` | Fails because `sqlite3` exposes `connect()`, not `open()`, for opening a database. |\n| B | `conn = connect.sqlite3('hello.db')` | Fails because the function belongs to the `sqlite3` module; the expression reverses the module and function names. |\n| C | `conn = sqlite3.connect_db('hello.db')` | Fails because there is no `connect_db()` function in the `sqlite3` module. |\n\nThis line opens a connection, but it does not create any tables or execute SQL by itself. Those operations require later calls through `conn` or a cursor."
  },
  {
    "type": "single",
    "question": "Part 3 of 4: What is true immediately after this program runs?\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('hello.db')\n```",
    "options": [
      "`conn` is a `sqlite3.Connection` object for the on-disk database `hello.db`; the file is created if it does not exist.",
      "`conn` is a `sqlite3.Cursor` object that is already positioned on the first query result.",
      "The program raises `FileNotFoundError` whenever `hello.db` does not already exist.",
      "`conn` is the string `'hello.db'`, because `connect()` returns the argument passed to it."
    ],
    "answer": [
      0
    ],
    "explanation": "Incorrect - the correct answer is A: `conn` is a `sqlite3.Connection` object for the on-disk database `hello.db`; the file is created if it does not exist.\n\nExplanation\n\nThe complete program is:\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('hello.db')\n```\n\n`sqlite3.connect()` returns a `Connection` object. Because `'hello.db'` is a relative file path, the connection targets a disk-backed SQLite database in the current working directory. If the file is absent, SQLite creates it as part of opening the connection. The call does not return the filename, and it does not create a cursor or execute a query.\n\nStep-by-step state changes:\n\n| Step | Operation | State or result |\n|------|-----------|-----------------|\n| 1 | `import sqlite3` | The module is bound to the name `sqlite3`. |\n| 2 | Evaluate `'hello.db'` | The database argument is a relative path. |\n| 3 | Call `sqlite3.connect(...)` | SQLite opens the file or creates it if missing. |\n| 4 | Assign the return value | `conn` refers to a `sqlite3.Connection` object. |\n\nNo SQL statement has run yet, so the database has no table merely because this connection was opened. A cursor can be created later, for example:\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('hello.db')\ncur = conn.cursor()\ncur.execute('CREATE TABLE notes(text)')\nconn.commit()\nconn.close()\n```\n\nWhy the other options fail:\n\n- **B** is wrong because `connect()` returns a `Connection`; a cursor must be requested with `conn.cursor()` or obtained through a connection shortcut.\n- **C** is wrong because opening a nonexistent database file is a supported behavior of `sqlite3.connect()`; it does not require the file to exist first.\n- **D** is wrong because the string is only the input argument. The return value is the connection object."
  },
  {
    "type": "single",
    "question": "Part 4 of 4: Which expression should replace the blank to create a database that exists only in memory?\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect(____)\n```",
    "options": [
      "`':memory'`",
      "`'hello.db'`",
      "`':memory:'`",
      "`'memory'`"
    ],
    "answer": [
      2
    ],
    "explanation": "Incorrect - the correct answer is C: `':memory:'`.\n\nExplanation\n\nSQLite gives the exact string `':memory:'` a special meaning when it is passed as the database argument. It creates a temporary database stored in memory rather than a database file on disk. The completed program is:\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect(':memory:')\n```\n\nStep by step:\n\n1. `import sqlite3` loads the standard-library SQLite interface.\n2. Python evaluates the exact string `':memory:'`.\n3. `sqlite3.connect(':memory:')` recognizes the special SQLite database name.\n4. A new in-memory database is created and a `sqlite3.Connection` object is returned.\n5. `conn` refers to that connection; the database lasts while this connection remains open.\n6. Closing the connection releases the in-memory database, so its data is not available through a later independent connection.\n\nA small runnable example is:\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect(':memory:')\nconn.execute('CREATE TABLE notes(text)')\nconn.execute('INSERT INTO notes VALUES (?)', ('temporary',))\nconn.commit()\nprint(conn.execute('SELECT text FROM notes').fetchone())  # ('temporary',)\nconn.close()\n```\n\nThe database is not a permanent file. Also, two separate calls create two separate in-memory databases unless a shared-memory URI is deliberately used.\n\nWhy the other options fail:\n\n| Option | Expression | Result |\n|--------|------------|--------|\n| C | `':memory:'` | Correct: the exact special name selects a private in-memory database. |\n| A | `':memory'` | Not the special name because the final colon is missing. |\n| B | `'hello.db'` | Selects the disk-backed database file used in the previous parts. |\n| D | `'memory'` | Is an ordinary database name, not SQLite's special in-memory name. |"
  },
  {
    "type": "single",
    "question": "Which statement best describes SQLite's support for the SQL-92 standard?",
    "options": [
      "SQLite fully implements every SQL-92 feature without any omissions or extensions.",
      "SQLite generally implements the common SQL-92 features, but some standard features are omitted.",
      "SQLite does not implement SQL; it accepts only commands unique to SQLite.",
      "SQLite implements only SQL-92 transaction statements and does not support queries or table definitions."
    ],
    "answer": [
      1
    ],
    "explanation": "Incorrect - the correct answer is B: SQLite generally implements the common SQL-92 features, but some standard features are omitted.\n\nExplanation\n\nSQLite's official documentation says that it understands most of the standard SQL language, while also noting that it omits some features and adds a few of its own. For example, SQLite does not provide complete `ALTER TABLE` support, `GRANT` and `REVOKE`, or direct writes to views. Therefore, saying that SQLite generally implements SQL-92 is reasonable as a broad description, but it does not mean complete, strict conformance to every SQL-92 feature.\n\nWhy the other options are wrong:\n\n| Option | Why it is right or wrong |\n|--------|--------------------------|\n| B | Correct: SQLite supports most common standard SQL features, but it has documented omissions and SQLite-specific extensions. |\n| A | Wrong because \"every\" and \"without any omissions or extensions\" contradict SQLite's documentation. |\n| C | Wrong because SQLite is an SQL database engine and supports standard statements such as `SELECT`, `CREATE TABLE`, `INSERT`, `UPDATE`, and `DELETE`. |\n| D | Wrong because SQLite supports both queries and table-definition statements in addition to transaction statements. |\n\nSource: [SQLite Query Language](https://sqlite.org/lang.html) and [SQL Features That SQLite Does Not Implement](https://sqlite.org/omitted.html)."
  },
  {
    "type": "single",
    "question": "Part 1 of 4: Which line starts a `CREATE TABLE` statement for a table named `tasks`?\n\nThis is a progressive question set: each part adds to the same SQL statement, and later parts reveal the answers to earlier parts. Answer the parts in order.\n\n```sql\n-- Write the first line of the statement:\n```",
    "options": [
      "`CREATE tasks TABLE (`",
      "`TABLE CREATE tasks (`",
      "`CREATE TABLE (tasks`",
      "`CREATE TABLE tasks (`"
    ],
    "answer": [
      3
    ],
    "explanation": "Incorrect - the correct answer is D: `CREATE TABLE tasks (`.\n\nExplanation\n\nSQLite uses the form `CREATE TABLE table_name (` to begin a table definition. `CREATE TABLE` is the command, `tasks` is the table name, and the opening parenthesis begins the list of column definitions. The completed program so far is:\n\n```sql\nCREATE TABLE tasks (\n```\n\nWhy the other options fail:\n\n| Option | Statement | Result |\n|--------|-----------|--------|\n| D | `CREATE TABLE tasks (` | Correct: it uses the required command, table name, and opening parenthesis in the correct order. |\n| A | `CREATE tasks TABLE (` | Wrong keyword order: `TABLE` must immediately follow `CREATE`. |\n| B | `TABLE CREATE tasks (` | Wrong keyword order: a `CREATE TABLE` statement starts with `CREATE`, not `TABLE`. |\n| C | `CREATE TABLE (tasks` | Wrong placement: the table name must come before the opening parenthesis. |\n\nThe next parts add column definitions inside the parentheses. SQLite's official `CREATE TABLE` syntax requires a table name and then one or more column definitions."
  },
  {
    "type": "single",
    "question": "Part 2 of 4: Which line defines `id` as an integer primary key?\n\n```sql\nCREATE TABLE tasks (\n-- Write the first column definition:\n```",
    "options": [
      "`id INTEGER PRIMARY KEY,`",
      "`id PRIMARY KEY INTEGER,`",
      "`INTEGER id PRIMARY KEY,`",
      "`id INTEGER UNIQUE,`"
    ],
    "answer": [
      0
    ],
    "explanation": "Incorrect - the correct answer is A: `id INTEGER PRIMARY KEY,`.\n\nExplanation\n\nA column definition starts with the column name, followed by its declared type and then its constraints. `id INTEGER PRIMARY KEY` therefore declares an integer primary-key column. The comma is required here because more column definitions follow. The completed program so far is:\n\n```sql\nCREATE TABLE tasks (\nid INTEGER PRIMARY KEY,\n```\n\nIn SQLite, a single-column primary key whose declared type is exactly `INTEGER` has the special integer-primary-key behavior and acts as an alias for the rowid in an ordinary rowid table. If an insert omits this value or supplies `NULL`, SQLite can generate an integer key automatically.\n\nWhy the other options fail:\n\n| Option | Statement | Result |\n|--------|-----------|--------|\n| A | `id INTEGER PRIMARY KEY,` | Correct: it uses the order column name, type, constraint, then comma. |\n| B | `id PRIMARY KEY INTEGER,` | Wrong order: the declared type comes before the column constraint. |\n| C | `INTEGER id PRIMARY KEY,` | Wrong order: the column name must come first. |\n| D | `id INTEGER UNIQUE,` | Wrong constraint: `UNIQUE` prevents duplicate values but does not declare the column as the table's primary key. |\n\nThe comma after the definition separates `id` from the next column."
  },
  {
    "type": "single",
    "question": "Part 3 of 4: Which line defines `name` as text and disallows `NULL` values?\n\n```sql\nCREATE TABLE tasks (\nid INTEGER PRIMARY KEY,\n\n-- Write the next column definition:\n```",
    "options": [
      "`name NOT NULL TEXT,`",
      "`TEXT name NOT NULL,`",
      "`name TEXT NOT NULL,`",
      "`name TEXT REQUIRED,`"
    ],
    "answer": [
      2
    ],
    "explanation": "Incorrect - the correct answer is C: `name TEXT NOT NULL,`.\n\nExplanation\n\nThe column definition places the name first, the declared type second, and the constraint after the type. `TEXT` gives the column text affinity, while `NOT NULL` rejects an insert or update that tries to store `NULL` in `name`. The comma separates this definition from the final column. The completed program so far is:\n\n```sql\nCREATE TABLE tasks (\nid INTEGER PRIMARY KEY,\nname TEXT NOT NULL,\n```\n\nWhy the other options fail:\n\n| Option | Statement | Result |\n|--------|-----------|--------|\n| C | `name TEXT NOT NULL,` | Correct: it declares `name` with type `TEXT` and attaches the `NOT NULL` constraint. |\n| A | `name NOT NULL TEXT,` | Wrong order: the declared type must appear before the column constraint. |\n| B | `TEXT name NOT NULL,` | Wrong order: the column name must be the first item in the definition. |\n| D | `name TEXT REQUIRED,` | Wrong constraint keyword: SQLite uses `NOT NULL`, not `REQUIRED`, for this rule. |\n\n`NOT NULL` is a column constraint. It is checked when rows are inserted or updated, not merely when the table is queried."
  },
  {
    "type": "single",
    "question": "Part 4 of 4: Which option adds the final `priority` column and closes the `CREATE TABLE` statement?\n\n```sql\nCREATE TABLE tasks (\nid INTEGER PRIMARY KEY,\nname TEXT NOT NULL,\n\n-- Write the last column definition and close the statement:\n```",
    "options": [
      "`priority INTEGER NULL\n);`",
      "`priority INTEGER NOT NULL\n);`",
      "`priority INTEGER NOT NULL,\n);`",
      "`priority TEXT NOT NULL\n);`"
    ],
    "answer": [
      1
    ],
    "explanation": "Incorrect - the correct answer is B: `priority INTEGER NOT NULL` followed by `);`.\n\nExplanation\n\nThe final column must be declared as `priority INTEGER NOT NULL`. Because it is the last column, it is not followed by a comma. The closing parenthesis ends the column-definition list, and the semicolon ends the SQL statement. The complete program is:\n\n```sql\nCREATE TABLE tasks (\nid INTEGER PRIMARY KEY,\nname TEXT NOT NULL,\npriority INTEGER NOT NULL\n);\n```\n\nWhy the other options fail:\n\n| Option | Statement | Result |\n|--------|-----------|--------|\n| B | `priority INTEGER NOT NULL` followed by `);` | Correct: it uses the requested type and constraint, then closes the table definition without a trailing comma. |\n| A | `priority INTEGER NULL` followed by `);` | Wrong because it explicitly permits `NULL`, contrary to the requested `NOT NULL` constraint. |\n| C | `priority INTEGER NOT NULL,` followed by `);` | Wrong because a trailing comma leaves the column list with a missing definition before the closing parenthesis. |\n| D | `priority TEXT NOT NULL` followed by `);` | Wrong because the requested declared type is `INTEGER`, not `TEXT`. |\n\nThe final table has three columns: `id` is an integer primary key, `name` is required text, and `priority` is required integer. SQLite documents `PRIMARY KEY` and `NOT NULL` as column constraints supported by `CREATE TABLE`."
  },
  {
    "type": "single",
    "question": "Which line should replace the blank so that `c.execute(...)` can run the `CREATE TABLE` statement?\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\n____\nc.execute('''CREATE TABLE tasks (\nid INTEGER PRIMARY KEY,\nname TEXT NOT NULL,\npriority INTEGER NOT NULL\n);''')\n```",
    "options": [
      "`c = sqlite3.cursor(conn)`",
      "`c = conn.connect()`",
      "`c = conn.cursor()`",
      "`c = cursor.conn()`"
    ],
    "answer": [
      2
    ],
    "explanation": "Incorrect - the correct answer is C: `c = conn.cursor()`.\n\nExplanation\n\n`sqlite3.connect('todo.db')` returns a `sqlite3.Connection` object. The connection's `cursor()` method creates and returns a `sqlite3.Cursor` object, which is then stored in `c`. The later call `c.execute(...)` uses that cursor to send the SQL statement to SQLite. The completed program is:\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\nc.execute('''CREATE TABLE tasks (\nid INTEGER PRIMARY KEY,\nname TEXT NOT NULL,\npriority INTEGER NOT NULL\n);''')\n```\n\nStep by step:\n\n1. `import sqlite3` loads Python's standard-library SQLite interface.\n2. `sqlite3.connect('todo.db')` opens the database file or creates it if it does not exist, returning a `Connection`.\n3. `conn.cursor()` creates a `Cursor` associated with that connection.\n4. `c.execute(...)` uses the cursor to execute the `CREATE TABLE` statement.\n\nWhy the other options fail:\n\n| Option | Statement | Result |\n|--------|-----------|--------|\n| C | `c = conn.cursor()` | Correct: calls the `Connection` method that creates a cursor. |\n| A | `c = sqlite3.cursor(conn)` | Wrong: `cursor()` is a method of the connection object, not a module-level `sqlite3` function. |\n| B | `c = conn.connect()` | Wrong: the connection has no `connect()` method; `sqlite3.connect()` created the connection. |\n| D | `c = cursor.conn()` | Wrong: `cursor` has not been defined, and the object relationship is reversed. |\n\nThe Python documentation describes `Connection.cursor()` as creating and returning a `Cursor` object. Source: [Python sqlite3 documentation](https://docs.python.org/3/library/sqlite3.html#sqlite3.Connection.cursor)."
  },
  {
    "type": "single",
    "question": "Part 1 of 4: Which line starts an `INSERT` statement that targets the existing `tasks` table?\n\nThis is a progressive question set: each part adds to the same SQL statement, and later parts reveal the answers to earlier parts. Answer the parts in order.\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\n\nc.execute('''\n-- Write the first line of the SQL statement:\n''')\n```",
    "options": [
      "`INSERT tasks INTO (`",
      "`INSERT INTO tasks (`",
      "`INSERT TABLE tasks (`",
      "`INTO INSERT tasks (`"
    ],
    "answer": [
      1
    ],
    "explanation": "Incorrect - the correct answer is B: `INSERT INTO tasks (`.\n\nExplanation\n\nThe ordinary `INSERT` form begins with `INSERT INTO`, followed by the name of the existing table. The opening parenthesis starts the optional list of columns that will receive the values. The program so far is:\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\n\nc.execute('''\nINSERT INTO tasks (\n''')\n```\n\nWhy the other options fail:\n\n| Option | Statement | Result |\n|--------|-----------|--------|\n| B | `INSERT INTO tasks (` | Correct: it uses the `INSERT INTO table` syntax and begins the column list. |\n| A | `INSERT tasks INTO (` | Wrong keyword order: `INTO` must follow `INSERT`, and the table name follows `INTO`. |\n| C | `INSERT TABLE tasks (` | Wrong syntax: `TABLE` is not used between `INSERT` and the table name. |\n| D | `INTO INSERT tasks (` | Wrong keyword order: the statement must start with `INSERT`. |\n\nThis statement inserts rows into an existing table; it does not create the table. SQLite's `INSERT INTO table VALUES(...)` form is the standard form used by the remaining parts."
  },
  {
    "type": "single",
    "question": "Part 2 of 4: Which fragment supplies the requested `id, name, priority` column list in that order?\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\n\nc.execute('''\nINSERT INTO tasks (\n-- Write the column list and close the parenthesis:\n''')\n```",
    "options": [
      "`id, priority, name)`",
      "`id name priority)`",
      "`id, name, priority)`",
      "`id, name, priority`"
    ],
    "answer": [
      2
    ],
    "explanation": "Incorrect - the correct answer is C: `id, name, priority)`.\n\nExplanation\n\nThe column list names the target columns in the order that the later `VALUES` expressions will supply their data. The list must include the three columns from the requested statement and must close with `)`. The program so far is:\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\n\nc.execute('''\nINSERT INTO tasks (id, name, priority)\n''')\n```\n\nWhy the other options fail:\n\n| Option | Fragment | Result |\n|--------|----------|--------|\n| C | `id, name, priority)` | Correct: it names all three target columns and closes the column list. |\n| A | `id, priority, name)` | Syntactically possible, but it changes the required column order; it does not build the requested statement. |\n| B | `id name priority)` | Wrong: column names in the list must be separated by commas. |\n| D | `id, name, priority` | Incomplete: the opening parenthesis from Part 1 has not been closed. |\n\nWhen a column list is specified, SQLite matches each value with the corresponding named column by position."
  },
  {
    "type": "single",
    "question": "Part 3 of 4: Which keyword and punctuation introduce the row values?\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\n\nc.execute('''\nINSERT INTO tasks (id, name, priority)\n-- Write the next SQL fragment:\n''')\n```",
    "options": [
      "`VALUES (`",
      "`VALUE [`",
      "`SET (`",
      "`VALUES [`"
    ],
    "answer": [
      0
    ],
    "explanation": "Incorrect - the correct answer is A: `VALUES (`.\n\nExplanation\n\nThe `VALUES` keyword introduces one or more rows of literal or parameterized expressions, and the opening parenthesis begins the first row. The program so far is:\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\n\nc.execute('''\nINSERT INTO tasks (id, name, priority)\nVALUES (\n''')\n```\n\nWhy the other options fail:\n\n| Option | Fragment | Result |\n|--------|----------|--------|\n| A | `VALUES (` | Correct: it starts the row-value list for the `INSERT INTO ... VALUES` form. |\n| B | `VALUE [` | Wrong keyword and delimiters: SQLite uses `VALUES` and parentheses. |\n| C | `SET (` | Wrong form: `SET` belongs to `UPDATE`, not this `INSERT ... VALUES` statement. |\n| D | `VALUES [` | Wrong delimiter: row values are enclosed in parentheses, not square brackets. |\n\nThe number and order of values in the row must match the three columns listed in Part 2."
  },
  {
    "type": "single",
    "question": "Part 4 of 4: Which option supplies one valid row and completes the `INSERT` statement?\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\n\nc.execute('''\nINSERT INTO tasks (id, name, priority)\nVALUES (\n-- Write the values and finish the SQL statement:\n''')\n```",
    "options": [
      "`1, 'My first task');`",
      "`1, 'My first task', NULL);`",
      "`1, 'My first task', 1, 0);`",
      "`1, 'My first task', 1);`"
    ],
    "answer": [
      3
    ],
    "explanation": "Incorrect - the correct answer is D: `1, 'My first task', 1);`.\n\nExplanation\n\nThe column list contains three columns, so the row must provide three values in the same order: `id`, `name`, and `priority`. The first value is the integer `1`, the second is the text literal `'My first task'`, and the third is the integer `1`. The closing parenthesis ends the row, and the semicolon terminates the SQL statement. The complete program is:\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\nc.execute('''INSERT INTO tasks (id, name, priority)\nVALUES (1, 'My first task', 1);''')\nconn.commit()\n```\n\nStep by step:\n\n1. `INSERT INTO tasks` selects the existing `tasks` table.\n2. The column list maps the first, second, and third values to `id`, `name`, and `priority`.\n3. `VALUES (1, 'My first task', 1)` supplies one complete row.\n4. `c.execute(...)` sends the statement to SQLite.\n5. `conn.commit()` commits the data change so it is retained when the connection is later closed.\n\nWhy the other options fail:\n\n| Option | Values | Result |\n|--------|--------|--------|\n| D | `1, 'My first task', 1);` | Correct: three non-NULL values match the three listed columns and their declared types. |\n| A | `1, 'My first task');` | Wrong: only two values are supplied for three listed columns. |\n| B | `1, 'My first task', NULL);` | Wrong: `priority` is declared `NOT NULL`, so this row violates the table constraint. |\n| C | `1, 'My first task', 1, 0);` | Wrong: four values are supplied for three listed columns. |\n\nSource: [SQLite INSERT documentation](https://sqlite.org/lang_insert.html)."
  },
  {
    "type": "single",
    "question": "Which expression should replace `____` so that the two `?` placeholders receive the intended values safely?\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\nc.execute('''CREATE TABLE IF NOT EXISTS tasks (\nid INTEGER PRIMARY KEY,\nname TEXT NOT NULL,\npriority INTEGER NOT NULL\n);''')\nc.execute('INSERT INTO tasks (name, priority) VALUES (?,?)', ____)\n```",
    "options": [
      "`('My first task')`",
      "`('My first task', 1)`",
      "`['My first task', 1, 0]`",
      "`['My first task']`"
    ],
    "answer": [
      1
    ],
    "explanation": "Incorrect - the correct answer is B: `('My first task', 1)`.\n\nExplanation\n\nThe SQL statement contains two qmark placeholders: the first `?` corresponds to `name`, and the second `?` corresponds to `priority`. The parameters argument must therefore contain exactly two values in that order. The completed insertion is:\n\n```python\nc.execute(\n    'INSERT INTO tasks (name, priority) VALUES (?,?)',\n    ('My first task', 1)\n)\n```\n\nThe values are bound separately from the SQL text. They are not assembled into the query by string concatenation, which avoids treating data as executable SQL and helps protect against SQL injection. `name` receives the string `My first task`, while `priority` receives the integer `1`.\n\nWhy the other options fail:\n\n| Option | Expression | Result |\n|--------|------------|--------|\n| B | `('My first task', 1)` | Correct: it is a two-item tuple whose values match the two placeholders from left to right. |\n| A | `('My first task')` | Wrong: parentheses alone do not create a tuple; this is a string, so its characters are treated as a sequence with the wrong number of bindings. |\n| C | `['My first task', 1, 0]` | Wrong: a list is an acceptable parameter sequence, but it contains three values for two placeholders. |\n| D | `['My first task']` | Wrong: a list is an acceptable parameter sequence, but it contains only one value for two placeholders. |\n\nAfter a successful `INSERT`, call `conn.commit()` when the change should be persisted to the database file:\n\n```python\nconn.commit()\n```\n\nSource: [Python sqlite3 documentation - How to use placeholders to bind values in SQL queries](https://docs.python.org/3/library/sqlite3.html#how-to-use-placeholders-to-bind-values-in-sql-queries)."
  },
  {
    "type": "single",
    "question": "Part 1 of 2: Which line should be added after the `INSERT` so the new task is saved?\n\nThis is a progressive question set: each part adds to the same program, and the later part reveals the answer to this part. Answer the parts in order.\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\nc.execute('''CREATE TABLE IF NOT EXISTS tasks (\nid INTEGER PRIMARY KEY,\nname TEXT NOT NULL,\npriority INTEGER NOT NULL\n);''')\nc.execute('INSERT INTO tasks (name, priority) VALUES (?,?)', ('My first task', 1))\n\n# Write the next line:\n```",
    "options": [
      "`conn.commit()`",
      "`conn.close()`",
      "`c.commit()`",
      "`conn.save()`"
    ],
    "answer": [
      0
    ],
    "explanation": "Incorrect - the correct answer is A: `conn.commit()`.\n\nExplanation\n\nThe `INSERT` changes the database, so the pending transaction must be committed through the `Connection` object. The program so far is:\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\nc.execute('''CREATE TABLE IF NOT EXISTS tasks (\nid INTEGER PRIMARY KEY,\nname TEXT NOT NULL,\npriority INTEGER NOT NULL\n);''')\nc.execute('INSERT INTO tasks (name, priority) VALUES (?,?)', ('My first task', 1))\nconn.commit()\n```\n\n`conn.commit()` makes the successful `INSERT` durable in the database file. It is called on the connection, not on the cursor. The next part closes this same connection after the commit.\n\nWhy the other options fail:\n\n| Option | Statement | Result |\n|--------|-----------|--------|\n| A | `conn.commit()` | Correct: commits pending changes on the database connection. |\n| B | `conn.close()` | Wrong at this point: closing is cleanup, not the explicit commit needed to save the pending `INSERT`. |\n| C | `c.commit()` | Wrong: the cursor executes statements, but transaction control belongs to the connection. |\n| D | `conn.save()` | Wrong: `sqlite3.Connection` has no `save()` method for committing a transaction. |\n\nThe Python `sqlite3` documentation recommends calling `Connection.commit()` after data-changing statements when the changes should be persisted."
  },
  {
    "type": "single",
    "question": "Part 2 of 2: After committing the `INSERT`, which final line closes the database connection?\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\nc.execute('''CREATE TABLE IF NOT EXISTS tasks (\nid INTEGER PRIMARY KEY,\nname TEXT NOT NULL,\npriority INTEGER NOT NULL\n);''')\nc.execute('INSERT INTO tasks (name, priority) VALUES (?,?)', ('My first task', 1))\nconn.commit()\n\n# Write the final line:\n```",
    "options": [
      "`c.close_connection()`",
      "`sqlite3.close(conn)`",
      "`conn.close()`",
      "`conn.disconnect()`"
    ],
    "answer": [
      2
    ],
    "explanation": "Incorrect - the correct answer is C: `conn.close()`.\n\nExplanation\n\n`conn` refers to the `sqlite3.Connection` object returned by `sqlite3.connect()`. Calling its `close()` method releases the database connection. The complete program is:\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\nc.execute('''CREATE TABLE IF NOT EXISTS tasks (\nid INTEGER PRIMARY KEY,\nname TEXT NOT NULL,\npriority INTEGER NOT NULL\n);''')\nc.execute('INSERT INTO tasks (name, priority) VALUES (?,?)', ('My first task', 1))\nconn.commit()\nconn.close()\n```\n\nStep by step:\n\n1. `sqlite3.connect('todo.db')` creates the `Connection` object stored in `conn`.\n2. The cursor executes the table creation and parameterized `INSERT`.\n3. `conn.commit()` saves the pending data change.\n4. `conn.close()` releases the connection after the database work is complete.\n\nWhy the other options fail:\n\n| Option | Statement | Result |\n|--------|-----------|--------|\n| C | `conn.close()` | Correct: `close()` is a method of the `Connection` object. |\n| A | `c.close_connection()` | Wrong: the cursor does not expose a method with this name; it also does not close the connection. |\n| B | `sqlite3.close(conn)` | Wrong: the module has no module-level `close()` function for an existing connection. |\n| D | `conn.disconnect()` | Wrong: `Connection` uses `close()`, not `disconnect()`. |\n\nClosing the connection after committing is good resource-management practice. The explicit commit must happen first so that the `INSERT` is not left pending when the connection is closed. Source: [Python sqlite3 documentation](https://docs.python.org/3/library/sqlite3.html#sqlite3.Connection.close)."
  },
  {
    "type": "single",
    "question": "Which block should replace the blank to insert both task rows with `c.executemany()` and a list of parameter tuples?\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\nc.execute('''CREATE TABLE IF NOT EXISTS tasks (\nid INTEGER PRIMARY KEY,\nname TEXT NOT NULL,\npriority INTEGER NOT NULL\n);''')\n\n# Insert both rows here:\n____\n\nconn.commit()\nconn.close()\n```",
    "options": [
      "`task_rows = ('My first task', 1)\nc.execute('INSERT INTO tasks (name, priority) VALUES (?, ?)', task_rows)`",
      "`task_rows = [\n    ('My first task', 1),\n    ('Second task', 2),\n]\nc.execute('INSERT INTO tasks (name, priority) VALUES (?, ?)', task_rows)`",
      "`task_rows = [\n    ('My first task', 1),\n    ('Second task', 2),\n]\nc.executemany('INSERT INTO tasks (name, priority) VALUES (?, ?)', ('My first task', 1))`",
      "`task_rows = [\n    ('My first task', 1),\n    ('Second task', 2),\n]\nc.executemany(\n    'INSERT INTO tasks (name, priority) VALUES (?, ?)',\n    task_rows\n)`"
    ],
    "answer": [
      3
    ],
    "explanation": "Incorrect - the correct answer is D: a list of row tuples passed to `c.executemany(...)`.\n\nExplanation\n\n`Cursor.executemany(operation, seq_of_parameters)` runs the same parameterized SQL operation once for every item in the supplied sequence. The correct block uses an outer list containing one tuple per row:\n\n```python\ntask_rows = [\n    ('My first task', 1),\n    ('Second task', 2),\n]\nc.executemany(\n    'INSERT INTO tasks (name, priority) VALUES (?, ?)',\n    task_rows\n)\n```\n\nStep by step:\n\n1. `task_rows` is a list with two elements.\n2. Each element is a two-item tuple matching `name` and `priority`.\n3. `executemany()` binds the first tuple to the first execution and the second tuple to the second execution.\n4. The two rows are inserted using the same SQL statement and its two `?` placeholders.\n5. `conn.commit()` after the block saves both inserted rows.\n\nWhy the other options fail:\n\n| Option | Result |\n|--------|--------|\n| D | Correct: it passes a list of parameter tuples to `executemany()`, so one row is inserted for each tuple. |\n| A | Wrong: it uses `execute()` for one tuple, so it inserts only one row and does not use the requested `executemany()` pattern. |\n| B | Wrong: `execute()` expects parameters for one execution; passing the outer list does not execute the statement once per row. |\n| C | Wrong: `executemany()` receives one row tuple instead of the outer sequence of row tuples, so it does not receive two complete parameter sets. |\n\nUsing `?` placeholders keeps the data separate from the SQL text and avoids constructing SQL with string concatenation. Source: [Python sqlite3 documentation - Cursor.executemany](https://docs.python.org/3/library/sqlite3.html#sqlite3.Cursor.executemany)."
  },
  {
    "type": "single",
    "question": "Part 1 of 10: Which declaration begins the refactoring by grouping the TODO application's database behavior in a class named `Todo`?\n\nThis is a progressive code set: every part builds on the same program, and later parts reveal the completed answers to earlier parts. Answer the parts in order.\n\n```python\nimport sqlite3\n\n# Write the class declaration:\n```",
    "options": [
      "`class Todo:`",
      "`def Todo:`",
      "`Todo = class:`",
      "`class todo():`"
    ],
    "answer": [
      0
    ],
    "explanation": "Incorrect - the correct answer is A: `class Todo:`.\n\nA class groups related state and behavior behind one object. The class name uses the `Todo` spelling from the target program, and calling `Todo()` later will create an application object. The completed program at this first step is:\n\n```python\nimport sqlite3\n\nclass Todo:\n    pass\n```\n\nConstruction trace:\n\n| Step | Operation | Result |\n|------|-----------|--------|\n| 1 | Execute `import sqlite3` | The SQLite module is bound to `sqlite3`. |\n| 2 | Execute `class Todo:` | Python creates a class object and binds it to `Todo`. |\n| 3 | Execute `pass` in the class body | The class is currently empty; no database connection is opened yet. |\n\nOption B starts a function definition and is also missing a parameter list and body. Option C is not Python class-definition syntax. Option D uses a lowercase name and does not match the requested `Todo` class; `class todo():` would be syntactically valid, but it would define a different class name and would not match the later `Todo()` call. Python's class tutorial describes a class definition as the construct used to bundle data and functionality together. Source: [Python Classes](https://docs.python.org/3/tutorial/classes.html)."
  },
  {
    "type": "single",
    "question": "Part 2 of 10: Which method should be added so that each `Todo()` object can initialize its own database state automatically?\n\n```python\nimport sqlite3\n\nclass Todo:\n    # Add the initialization method:\n```",
    "options": [
      "`def initialize(self):`",
      "`def __new__(self):`",
      "`def __init__(self):`",
      "`def init(self):`"
    ],
    "answer": [
      2
    ],
    "explanation": "Incorrect - the correct answer is C: `def __init__(self):`.\n\n`__init__()` is called automatically for the new instance when `Todo()` is instantiated. The completed program at this step is:\n\n```python\nimport sqlite3\n\nclass Todo:\n    def __init__(self):\n        pass\n```\n\nExecution trace:\n\n| Step | Code or event | Result |\n|------|---------------|--------|\n| 1 | Define `Todo` | Python stores the class and its `__init__` method. |\n| 2 | Evaluate `Todo()` later | Python creates an instance and invokes `Todo.__init__(instance)`. |\n| 3 | Run `pass` | The instance exists but has no database attributes yet. |\n\nOption A is an ordinary method; Python will not call it automatically. Option B is the object-creation hook `__new__()`, not the initialization method requested here, and its usual first parameter is the class (`cls`). Option D is just an ordinary method because the special double-underscore spelling is missing. The Python documentation states that defining `__init__()` lets a class customize a newly created instance. Source: [Python Classes](https://docs.python.org/3/tutorial/classes.html#class-objects)."
  },
  {
    "type": "single",
    "question": "Part 3 of 10: Which line opens `todo.db` and stores the connection on the current `Todo` instance?\n\n```python\nimport sqlite3\n\nclass Todo:\n    def __init__(self):\n        # Open todo.db and save the connection:\n```",
    "options": [
      "`self.conn = sqlite3.open('todo.db')`",
      "`self.conn = sqlite3.connect('todo.db')`",
      "`conn = sqlite3.connect('todo.db')`",
      "`self.conn = sqlite3.open_connection('todo.db')`"
    ],
    "answer": [
      1
    ],
    "explanation": "Incorrect - the correct answer is B: `self.conn = sqlite3.connect('todo.db')`.\n\nThe completed program is:\n\n```python\nimport sqlite3\n\nclass Todo:\n    def __init__(self):\n        self.conn = sqlite3.connect('todo.db')\n```\n\nExecution trace when `app = Todo()` runs:\n\n| Step | Operation | State or result |\n|------|-----------|-----------------|\n| 1 | Create the `Todo` instance | The instance becomes the `self` argument. |\n| 2 | Evaluate `'todo.db'` | A relative database filename is prepared. |\n| 3 | Call `sqlite3.connect(...)` | SQLite opens the existing file or creates it if it does not exist, returning a `Connection`. |\n| 4 | Assign through `self.conn` | The connection remains available to the instance's later methods. |\n\nOption A uses `open()`, which is not the SQLite module's connection function. Option C creates only a local name `conn`; later methods cannot use it through `self`. Option D calls nonexistent `open_connection()`, so it cannot open the database. The `sqlite3` documentation defines `sqlite3.connect()` as the function that opens a connection to a SQLite database. Source: [Python `sqlite3` documentation](https://docs.python.org/3/library/sqlite3.html)."
  },
  {
    "type": "single",
    "question": "Part 4 of 10: Which line creates a cursor from the saved connection and stores it as `self.c`?\n\n```python\nimport sqlite3\n\nclass Todo:\n    def __init__(self):\n        self.conn = sqlite3.connect('todo.db')\n        # Create and store the cursor:\n```",
    "options": [
      "`self.c = sqlite3.cursor(self.conn)`",
      "`self.c = self.conn.execute()`",
      "`c = self.conn.cursor()`",
      "`self.c = self.conn.cursor()`"
    ],
    "answer": [
      3
    ],
    "explanation": "Incorrect - the correct answer is D: `self.c = self.conn.cursor()`.\n\nThe completed program is:\n\n```python\nimport sqlite3\n\nclass Todo:\n    def __init__(self):\n        self.conn = sqlite3.connect('todo.db')\n        self.c = self.conn.cursor()\n```\n\nExecution trace:\n\n| Step | Operation | Result |\n|------|-----------|--------|\n| 1 | `sqlite3.connect('todo.db')` | `self.conn` refers to a `sqlite3.Connection`. |\n| 2 | `self.conn.cursor()` | The connection creates a `sqlite3.Cursor`. |\n| 3 | Assign to `self.c` | Both the connection and cursor can be reused by other instance methods. |\n\nOption A is wrong because `cursor()` is a method of the connection object, not a module-level `sqlite3` function. Option B calls `execute()` without the required SQL statement. Option C does create a cursor, but assigns it to a local `c`, so the `Todo` object would not retain it as `self.c`. The documented relationship is `Connection.cursor()` creates and returns a cursor. Source: [Python `sqlite3.Connection.cursor`](https://docs.python.org/3/library/sqlite3.html#sqlite3.Connection.cursor)."
  },
  {
    "type": "single",
    "question": "Part 5 of 10: Which call makes table creation part of every `Todo` object's initialization?\n\n```python\nimport sqlite3\n\nclass Todo:\n    def __init__(self):\n        self.conn = sqlite3.connect('todo.db')\n        self.c = self.conn.cursor()\n        # Call the table-creation method:\n```",
    "options": [
      "`self.create_tasks_table()`",
      "`self.create_task_table()`",
      "`create_tasks_table(self)`",
      "`self.create_tasks_table`"
    ],
    "answer": [
      0
    ],
    "explanation": "Incorrect - the correct answer is A: `self.create_tasks_table()`.\n\nThe completed initialization sequence is:\n\n```python\nimport sqlite3\n\nclass Todo:\n    def __init__(self):\n        self.conn = sqlite3.connect('todo.db')\n        self.c = self.conn.cursor()\n        self.create_tasks_table()\n```\n\nExecution trace for `app = Todo()`:\n\n| Step | State change |\n|------|--------------|\n| 1 | A `Todo` instance is created and passed as `self`. |\n| 2 | `self.conn` stores the database connection. |\n| 3 | `self.c` stores a cursor belonging to that connection. |\n| 4 | The parentheses call `create_tasks_table` immediately. |\n\nOption B has the singular name `create_task_table`, which does not match the requested plural method name and will fail once the method is defined with the plural spelling. Option C calls an unqualified name and manually supplies `self`; method calls on an instance should use `self.method()`. Option D only retrieves a bound method object and does not call it because the parentheses are missing. The exact method name must agree at both the definition and call sites."
  },
  {
    "type": "single",
    "question": "Part 6 of 10: Which definition matches the method called in `__init__` and allows it to use the instance cursor?\n\n```python\nimport sqlite3\n\nclass Todo:\n    def __init__(self):\n        self.conn = sqlite3.connect('todo.db')\n        self.c = self.conn.cursor()\n        self.create_tasks_table()\n\n    # Define the method called above:\n```",
    "options": [
      "`def create_tasks_table():`",
      "`def create_task_table(self):`",
      "`def create_tasks_table(self):`",
      "`def create_tasks_table(self, table_name):`"
    ],
    "answer": [
      2
    ],
    "explanation": "Incorrect - the correct answer is C: `def create_tasks_table(self):`.\n\nThe class now has a matching method definition and call:\n\n```python\nimport sqlite3\n\nclass Todo:\n    def __init__(self):\n        self.conn = sqlite3.connect('todo.db')\n        self.c = self.conn.cursor()\n        self.create_tasks_table()\n\n    def create_tasks_table(self):\n        pass\n```\n\nExecution trace:\n\n| Step | Operation | Result |\n|------|-----------|--------|\n| 1 | Define `create_tasks_table(self)` | The function is stored as a method on `Todo`. |\n| 2 | Call `self.create_tasks_table()` | Python supplies the current instance as `self`. |\n| 3 | Enter the method | The method can access `self.c` and execute SQL through that cursor. |\n\nOption A omits the instance parameter, so a normal instance call would raise a missing-argument `TypeError`. Option B uses a different singular name, so it does not satisfy the call in `__init__`. Option D requires an extra `table_name` argument that the call does not provide. The requested method name is plural, `create_tasks_table`, even though the original sample used the inconsistent singular spelling in one place; the definition and call must use one identical name."
  },
  {
    "type": "single",
    "question": "Part 7 of 10: Which statement executes a repeat-safe definition of the `tasks` table?\n\n```python\nimport sqlite3\n\nclass Todo:\n    def __init__(self):\n        self.conn = sqlite3.connect('todo.db')\n        self.c = self.conn.cursor()\n        self.create_tasks_table()\n\n    def create_tasks_table(self):\n        # Execute a repeat-safe table definition:\n```",
    "options": [
      "`self.c.execute('CREATE TABLE tasks (id INTEGER PRIMARY KEY, name TEXT NOT NULL, priority INTEGER NOT NULL)')`",
      "`self.c.execute('CREATE TABLE IF EXISTS tasks (id INTEGER PRIMARY KEY, name TEXT NOT NULL, priority INTEGER NOT NULL)')`",
      "`self.c.execute('CREATE IF NOT EXISTS TABLE tasks (id INTEGER PRIMARY KEY, name TEXT NOT NULL, priority INTEGER NOT NULL)')`",
      "`self.c.execute('CREATE TABLE IF NOT EXISTS tasks (id INTEGER PRIMARY KEY, name TEXT NOT NULL, priority INTEGER NOT NULL)')`"
    ],
    "answer": [
      3
    ],
    "explanation": "Incorrect - the correct answer is D: `self.c.execute('CREATE TABLE IF NOT EXISTS tasks (id INTEGER PRIMARY KEY, name TEXT NOT NULL, priority INTEGER NOT NULL)')`.\n\nThe completed class so far is:\n\n```python\nimport sqlite3\n\nclass Todo:\n    def __init__(self):\n        self.conn = sqlite3.connect('todo.db')\n        self.c = self.conn.cursor()\n        self.create_tasks_table()\n\n    def create_tasks_table(self):\n        self.c.execute('''CREATE TABLE IF NOT EXISTS tasks (\n            id INTEGER PRIMARY KEY,\n            name TEXT NOT NULL,\n            priority INTEGER NOT NULL\n        );''')\n```\n\nExecution trace when the method is called:\n\n| Step | SQL component | Effect |\n|------|---------------|--------|\n| 1 | `CREATE TABLE` | Requests a new table definition. |\n| 2 | `IF NOT EXISTS` | Makes the command do nothing instead of raising a table-exists error when `tasks` is already present. |\n| 3 | `id INTEGER PRIMARY KEY` | Defines the identifier column. |\n| 4 | `name TEXT NOT NULL` | Defines required text task names. |\n| 5 | `priority INTEGER NOT NULL` | Defines required integer priorities. |\n\nOption A can fail on a second initialization because it omits `IF NOT EXISTS`. Option B puts `IF EXISTS` after `TABLE`; that is not the `CREATE TABLE IF NOT EXISTS` syntax. Option C puts the keywords in the wrong order. SQLite documents that `CREATE TABLE IF NOT EXISTS` has no effect when a table or view of the same name already exists. Source: [SQLite `CREATE TABLE`](https://www.sqlite.org/lang_createtable.html)."
  },
  {
    "type": "single",
    "question": "Part 8 of 10: Which method reads both task values from the user instead of relying on hardcoded values?\n\n```python\nimport sqlite3\n\nclass Todo:\n    def __init__(self):\n        self.conn = sqlite3.connect('todo.db')\n        self.c = self.conn.cursor()\n        self.create_tasks_table()\n\n    def create_tasks_table(self):\n        self.c.execute('''CREATE TABLE IF NOT EXISTS tasks (\n            id INTEGER PRIMARY KEY,\n            name TEXT NOT NULL,\n            priority INTEGER NOT NULL\n        );''')\n\n    # Add the task-entry method:\n```",
    "options": [
      "`def add_task():\n    name = input('Enter task name: ')\n    priority = int(input('Enter priority: '))`",
      "`def add_task(self):\n    name = input('Enter task name: ')\n    priority = int(input('Enter priority: '))`",
      "`def add_task(self, name, priority):\n    pass`",
      "`def add_tasks(self):\n    name = input('Enter task name: ')\n    priority = int(input('Enter priority: '))`"
    ],
    "answer": [
      1
    ],
    "explanation": "Incorrect - the correct answer is B: `def add_task(self)` followed by the two `input()` calls.\n\nThe class now accepts values interactively:\n\n```python\nclass Todo:\n    # Earlier methods are unchanged.\n\n    def add_task(self):\n        name = input('Enter task name: ')\n        priority = int(input('Enter priority: '))\n```\n\nInput trace for the example responses `Review notes` and `3`:\n\n| Step | User response | Variable state |\n|------|---------------|----------------|\n| 1 | `Review notes` to the first prompt | `name == 'Review notes'` |\n| 2 | `3` to the second prompt | `input()` returns `'3'`, then `int()` converts it to `priority == 3` |\n\nOption A omits `self`, so calling it as `app.add_task()` would pass an unexpected instance argument. Option C expects the caller to supply values and therefore does not get them from the user as requested. Option D has the wrong method name and would not match the planned `app.add_task()` call. `input()` always returns text, so `int()` is needed when the database column should receive an integer priority. The SQL insertion is added in the next part."
  },
  {
    "type": "single",
    "question": "Part 9 of 10: Which block inserts the values collected by `add_task()` using SQLite placeholders?\n\n```python\nimport sqlite3\n\nclass Todo:\n    def __init__(self):\n        self.conn = sqlite3.connect('todo.db')\n        self.c = self.conn.cursor()\n        self.create_tasks_table()\n\n    def create_tasks_table(self):\n        self.c.execute('''CREATE TABLE IF NOT EXISTS tasks (\n            id INTEGER PRIMARY KEY,\n            name TEXT NOT NULL,\n            priority INTEGER NOT NULL\n        );''')\n\n    def add_task(self):\n        name = input('Enter task name: ')\n        priority = int(input('Enter priority: '))\n        # Insert name and priority safely:\n```",
    "options": [
      "`self.c.execute(\n    'INSERT INTO tasks (name, priority) VALUES (?, ?)',\n    (name, priority)\n)`",
      "`self.c.execute(\n    f'INSERT INTO tasks (name, priority) VALUES ({name}, {priority})'\n)`",
      "`self.c.execute(\n    'INSERT INTO tasks (name, priority) VALUES (?, ?)',\n    name, priority\n)`",
      "`self.c.execute(\n    'INSERT INTO tasks (name, priority) VALUES (?, ?)',\n    ('My first task', 1)\n)`"
    ],
    "answer": [
      0
    ],
    "explanation": "Incorrect - the correct answer is A: execute the SQL with two `?` placeholders and the parameter tuple `(name, priority)`.\n\nThe completed `add_task()` method is:\n\n```python\ndef add_task(self):\n    name = input('Enter task name: ')\n    priority = int(input('Enter priority: '))\n    self.c.execute(\n        'INSERT INTO tasks (name, priority) VALUES (?, ?)',\n        (name, priority)\n    )\n```\n\nBinding trace for `name == 'Review notes'` and `priority == 3`:\n\n| Step | SQL or value | Result |\n|------|--------------|--------|\n| 1 | SQL text contains `?, ?` | SQLite expects two values. |\n| 2 | Parameter tuple is `('Review notes', 3)` | The first value binds to `name`; the second binds to `priority`. |\n| 3 | `execute()` runs the `INSERT` | One row is added to the current transaction. |\n| 4 | No commit yet | The change is still pending and must be committed in the next part. |\n\nOption B constructs SQL with an f-string, mixing data into SQL text and failing to quote arbitrary task names safely. Option C passes two separate positional arguments after the parameter sequence; `execute()` expects one parameters argument. Option D has the correct shape but inserts hardcoded values rather than the values read from the user. The Python `sqlite3` documentation recommends placeholders and a separate sequence of values for parameter binding. Source: [SQLite parameter substitution in Python](https://docs.python.org/3/library/sqlite3.html#how-to-use-placeholders-to-bind-values-in-sql-queries)."
  },
  {
    "type": "single",
    "question": "Part 10 of 10: Which final block commits the new row, creates a `Todo` object, and starts the interactive task entry?\n\n```python\nimport sqlite3\n\nclass Todo:\n    def __init__(self):\n        self.conn = sqlite3.connect('todo.db')\n        self.c = self.conn.cursor()\n        self.create_tasks_table()\n\n    def create_tasks_table(self):\n        self.c.execute('''CREATE TABLE IF NOT EXISTS tasks (\n            id INTEGER PRIMARY KEY,\n            name TEXT NOT NULL,\n            priority INTEGER NOT NULL\n        );''')\n\n    def add_task(self):\n        name = input('Enter task name: ')\n        priority = int(input('Enter priority: '))\n        self.c.execute(\n            'INSERT INTO tasks (name, priority) VALUES (?, ?)',\n            (name, priority)\n        )\n        # Save the insert and run the application:\n```",
    "options": [
      "`self.c.close()\n\napp = Todo()\napp.add_task()`",
      "`self.c.commit()\n\napp = Todo()\napp.add_task()`",
      "`self.conn.commit()\n\napp = Todo()\napp.add_task()`",
      "`self.conn.save()\n\napp = Todo.add_task()`"
    ],
    "answer": [
      2
    ],
    "explanation": "Incorrect - the correct answer is C: commit through the connection, then instantiate `Todo` and call `add_task()`.\n\nThe complete refactored program is:\n\n```python\nimport sqlite3\n\nclass Todo:\n    def __init__(self):\n        self.conn = sqlite3.connect('todo.db')\n        self.c = self.conn.cursor()\n        self.create_tasks_table()\n\n    def create_tasks_table(self):\n        self.c.execute('''CREATE TABLE IF NOT EXISTS tasks (\n            id INTEGER PRIMARY KEY,\n            name TEXT NOT NULL,\n            priority INTEGER NOT NULL\n        );''')\n\n    def add_task(self):\n        name = input('Enter task name: ')\n        priority = int(input('Enter priority: '))\n        self.c.execute(\n            'INSERT INTO tasks (name, priority) VALUES (?, ?)',\n            (name, priority)\n        )\n        self.conn.commit()\n\napp = Todo()\napp.add_task()\n```\n\nEnd-to-end execution trace:\n\n| Step | Code | State or effect |\n|------|------|-----------------|\n| 1 | `app = Todo()` | `__init__()` opens `todo.db`, creates a cursor, and calls `create_tasks_table()`. |\n| 2 | `CREATE TABLE IF NOT EXISTS` | The `tasks` table is created if absent; an existing table is left in place. |\n| 3 | `app.add_task()` | The program asks for a task name and priority. |\n| 4 | `int(input(...))` | The priority text is converted to an integer. |\n| 5 | Parameterized `INSERT` | The two current variables are bound to `name` and `priority`, and one row is staged. |\n| 6 | `self.conn.commit()` | The staged row is committed to `todo.db`. |\n\nOption A closes the cursor without committing the pending insertion. Option B calls `commit()` on a cursor; transaction control belongs to the connection, not the cursor. Option D uses nonexistent or incorrectly called APIs: `Connection` has `commit()`, not `save()`, and `Todo.add_task` is an unbound method that is not the intended application call. The `sqlite3` documentation recommends committing before closing when pending changes must be retained. Source: [Python `sqlite3` documentation](https://docs.python.org/3/library/sqlite3.html)."
  },
  {
    "type": "single",
    "question": "Part 1 of 2: Which expression should replace `____` so the `for` loop iterates directly over the rows returned by SQLite?\n\nThis is a progressive code set: the next part builds on this completed program and reveals this answer. Answer the parts in order.\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\n\nfor row in ____:\n    print(row)\n\nconn.close()\n```",
    "options": [
      "`c.execute('SELECT * FROM tasks')`",
      "`c.execute('SELECT * FROM tasks').fetchone()`",
      "`c.execute('SELECT * FROM tasks').fetchall()[0]`",
      "`'SELECT * FROM tasks'`"
    ],
    "answer": [
      0
    ],
    "explanation": "Incorrect - the correct answer is A: `c.execute('SELECT * FROM tasks')`.\n\nThe completed program is:\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\n\nfor row in c.execute('SELECT * FROM tasks'):\n    print(row)\n\nconn.close()\n```\n\nA cursor is iterable. When the `for` statement asks it for the next item, the cursor executes the `SELECT` result iteration and yields one row tuple at a time. For example, if the table contains `(1, 'Read', 1)` and `(2, 'Test', 2)`, the execution trace is:\n\n| Round | Value assigned to `row` | Printed value |\n|-------|--------------------------|---------------|\n| 1 | `(1, 'Read', 1)` | `(1, 'Read', 1)` |\n| 2 | `(2, 'Test', 2)` | `(2, 'Test', 2)` |\n| 3 | No more rows | The cursor is exhausted and the loop ends normally. |\n\nOption B calls `fetchone()`, which returns one row tuple; a `for` loop would then iterate over that tuple's fields rather than over all rows. Option C takes the first row from `fetchall()`, with the same one-row problem and an additional failure when the query returns no rows. Option D is only a string, so the loop would iterate over its characters, not database rows. Python's `for` statement obtains successive items from an iterable, and the `sqlite3` documentation specifically describes cursors as iterable for `SELECT` results. Sources: [Python `for` statements](https://docs.python.org/3/tutorial/controlflow.html#for-statements) and [Python `sqlite3` documentation](https://docs.python.org/3/library/sqlite3.html#cursor-objects)."
  },
  {
    "type": "single",
    "question": "Part 2 of 2: Which expression should replace `____` so `result` is the cursor returned by `c.execute(...)`?\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\n\nfor row in c.execute('SELECT * FROM tasks'):\n    print(row)\n\nresult = ____\nfor row in result:\n    print(row)\n\nprint(type(result))\nprint(result is c)\nconn.close()\n```",
    "options": [
      "`c.fetchall('SELECT * FROM tasks')`",
      "`c.execute('SELECT * FROM tasks').fetchall()`",
      "`'SELECT * FROM tasks'`",
      "`c.execute('SELECT * FROM tasks')`"
    ],
    "answer": [
      3
    ],
    "explanation": "Incorrect - the correct answer is D: `c.execute('SELECT * FROM tasks')`.\n\nThe completed program is:\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\n\nresult = c.execute('SELECT * FROM tasks')\nfor row in result:\n    print(row)\n\nprint(type(result))\nprint(result is c)\nconn.close()\n```\n\n`Cursor.execute()` executes one SQL statement and returns the cursor itself. Therefore `result` has type `sqlite3.Cursor`, and `result is c` is `True`; the assignment creates a second name for the same cursor object, not a new list or a separate result object. The last two lines print:\n\n```text\n<class 'sqlite3.Cursor'>\nTrue\n```\n\nExecution trace:\n\n| Step | Operation | Result |\n|------|-----------|--------|\n| 1 | Create `c` with `conn.cursor()` | `c` is a `sqlite3.Cursor`. |\n| 2 | Call `c.execute('SELECT * FROM tasks')` | The cursor runs the query and returns itself. |\n| 3 | Assign the return value to `result` | `result` and `c` refer to the same cursor. |\n| 4 | Iterate over `result` | Each available database row is yielded as a tuple. |\n| 5 | Evaluate `type(result)` and `result is c` | The output is `sqlite3.Cursor` and `True`. |\n\nOption A is invalid because `fetchall()` is a cursor method for retrieving rows, not a method that accepts the SQL statement. Option B returns a list of all rows, so its type is `list` and it is not `c`. Option C only assigns a string and never executes SQL. The official `sqlite3` documentation states that `Cursor.execute()` returns the cursor itself. Source: [Python `sqlite3.Cursor.execute`](https://docs.python.org/3/library/sqlite3.html#sqlite3.Cursor.execute)."
  },
  {
    "type": "single",
    "question": "Part 1 of 2: Which expression should replace `____` so `rows` stores all rows remaining from the preceding `SELECT` query?\n\nThis is a progressive code set: the next part shows this completed code and asks about its memory behavior. Answer the parts in order.\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\nc.execute('SELECT * FROM tasks')\nrows = ____\nfor row in rows:\n    print(row)\nconn.close()\n```",
    "options": [
      "`c.fetchall()`",
      "`c.fetchone()`",
      "`c.execute('SELECT * FROM tasks')`",
      "`c.fetchone('SELECT * FROM tasks')`"
    ],
    "answer": [
      0
    ],
    "explanation": "Incorrect - the correct answer is A: `c.fetchall()`.\n\n`fetchall()` retrieves all remaining rows from the query result and returns them as a list. The completed program is:\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\nc.execute('SELECT * FROM tasks')\nrows = c.fetchall()\nfor row in rows:\n    print(row)\nconn.close()\n```\n\nExecution trace:\n\n| Step | Operation | Result |\n|------|-----------|--------|\n| 1 | `c.execute('SELECT * FROM tasks')` | The cursor executes the query and holds its result set. |\n| 2 | `c.fetchall()` | All remaining rows are consumed and returned in a new `list`, assigned to `rows`. |\n| 3 | `for row in rows` | The loop visits each row tuple in that list and prints it. |\n| 4 | `conn.close()` | The database connection is released after the rows have been processed. |\n\nOption B returns only the next single row tuple, or `None` when no row remains; it does not collect all rows. Option C returns the `sqlite3.Cursor`, not a list of fetched rows. Option D passes an argument to `fetchone()`, whose method does not accept the SQL statement; the query was already executed by the preceding `execute()` call. The official documentation defines `fetchall()` as returning all remaining rows as a list. Source: [Python `sqlite3.Cursor.fetchall`](https://docs.python.org/3/library/sqlite3.html#sqlite3.Cursor.fetchall)."
  },
  {
    "type": "single",
    "question": "Part 2 of 2: Which statement best explains the memory risk in this completed program and gives a safer approach for a very large `tasks` table?\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\nc.execute('SELECT * FROM tasks')\nrows = c.fetchall()\nfor row in rows:\n    print(row)\nconn.close()\n```",
    "options": [
      "`fetchall()` returns only the next row, so this program cannot run out of memory because of the query result.",
      "`fetchall()` returns all remaining rows in a list; a very large result can consume excessive memory or cause an out-of-memory failure, so iterate over the cursor directly or process bounded batches with `fetchmany()`.",
      "`fetchall()` returns the cursor itself, so `rows` is already a streaming iterator and the `for` loop cannot increase memory usage.",
      "The `for` statement copies `rows` once per iteration; replacing it with a `while` loop always prevents out-of-memory failures."
    ],
    "answer": [
      1
    ],
    "explanation": "Incorrect - the correct answer is B: `fetchall()` materializes all remaining rows as one list, which can exhaust memory for a very large result.\n\nThe code is functionally correct for a small or moderate result set, but its memory behavior is important:\n\n```python\nc.execute('SELECT * FROM tasks')\nrows = c.fetchall()  # Every remaining row is stored in this list at once.\nfor row in rows:\n    print(row)\n```\n\nMemory trace:\n\n| Step | Object or operation | Memory consequence |\n|------|---------------------|--------------------|\n| 1 | Execute the `SELECT` | The cursor has a result set ready to be read. |\n| 2 | Call `fetchall()` | Python creates a list containing every remaining row and its Python objects. |\n| 3 | Start the `for` loop | The list remains alive while the loop processes it; iteration does not release the whole list row by row. |\n| 4 | Result set is extremely large | The list may consume available memory and the process may raise `MemoryError` or be terminated by the operating system. |\n\nFor streaming-style processing, iterate over the cursor directly so the program does not first materialize a list of every row:\n\n```python\nc.execute('SELECT * FROM tasks')\nfor row in c:\n    print(row)\n```\n\nWhen bounded batches are needed, use `fetchmany()` instead:\n\n```python\nc.execute('SELECT * FROM tasks')\nwhile True:\n    batch = c.fetchmany(1000)\n    if not batch:\n        break\n    for row in batch:\n        print(row)\n```\n\nOption A is wrong because `fetchall()` returns all remaining rows, not one row. Option C is wrong because `fetchall()` returns a `list`, not a cursor. Option D is wrong because a normal `for` loop does not copy the entire list on every iteration, and changing loop syntax cannot undo the list already created by `fetchall()`. The official documentation states that `fetchall()` returns all remaining rows as a list and that cursors can be iterated for `SELECT` results. Source: [Python `sqlite3` documentation](https://docs.python.org/3/library/sqlite3.html#cursor-objects)."
  },
  {
    "type": "single",
    "question": "Which statement correctly identifies the type returned by `fetchall()` in this program?\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\nc.execute('SELECT * FROM tasks')\nrows = c.fetchall()\nprint(type(rows).__name__)\nprint(type(rows[0]).__name__)\nconn.close()\n```",
    "options": [
      "`rows` is a tuple and `rows[0]` is a list.",
      "`rows` is a cursor and `rows[0]` is a tuple.",
      "`rows` is a list and, with the default row factory, `rows[0]` is a tuple.",
      "`rows` is a dictionary and `rows[0]` is a string."
    ],
    "answer": [
      2
    ],
    "explanation": "Incorrect - the correct answer is C: `rows` is a `list`, and each row is a `tuple` by default.\n\n`c.fetchall()` retrieves all remaining rows and returns one list containing them. SQLite's default row factory represents each row as a tuple. For example, with the rows from the prompt, the value has this shape:\n\n```python\nrows = [\n    (1, 'My first task', 1),\n    (2, 'My second task', 5),\n    (3, 'My third task', 10),\n]\n```\n\nThe output is:\n\n```text\nlist\ntuple\n```\n\nStep by step:\n\n1. `c.execute(...)` returns the cursor and prepares the query result.\n2. `c.fetchall()` consumes all remaining rows and creates the outer `list`.\n3. `rows[0]` selects the first element of that list, which is the first row `tuple`.\n4. `rows[0][1]` would then select the task-name string from that tuple.\n\nOption A reverses the container and row types. Option B confuses the cursor with the value returned by `fetchall()`. Option D is not the default behavior; a dictionary-like result would require explicitly configuring a custom `row_factory`. Source: [Python `sqlite3` documentation](https://docs.python.org/3/library/sqlite3.html#sqlite3.Cursor.fetchall)."
  },
  {
    "type": "single",
    "question": "Which line correctly fetches all remaining rows after the `SELECT` statement?\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\nc.execute('SELECT * FROM tasks')\n# Write the next line:\n____\nconn.close()\n```",
    "options": [
      "`rows = c.fetchall()`",
      "`rows = c.fetchall('SELECT * FROM tasks')`",
      "`rows = c.execute.fetchall()`",
      "`rows = sqlite3.fetchall(c)`"
    ],
    "answer": [
      0
    ],
    "explanation": "Incorrect - the correct answer is A: `rows = c.fetchall()`.\n\nThe SQL has already been executed by `c.execute('SELECT * FROM tasks')`, so `fetchall()` is called on the cursor without the SQL string. The completed code is:\n\n```python\nc.execute('SELECT * FROM tasks')\nrows = c.fetchall()\nfor row in rows:\n    print(row)\n```\n\nExecution trace:\n\n| Step | Operation | Result |\n|------|-----------|--------|\n| 1 | `c.execute(...)` | The cursor prepares the query result. |\n| 2 | `c.fetchall()` | All remaining rows are returned as a list. |\n| 3 | `for row in rows` | Each row tuple can be processed. |\n\nOption B is invalid because `fetchall()` does not receive a SQL statement; it fetches the result of the query already executed on that cursor. Option C treats `execute` as though it were an object with a `fetchall` attribute instead of calling it through `c`. Option D is invalid because `fetchall()` is a cursor method, not a module-level `sqlite3` function. If no rows remain, the correct line still returns an empty list, `[]`. Source: [Python `sqlite3.Cursor.fetchall`](https://docs.python.org/3/library/sqlite3.html#sqlite3.Cursor.fetchall)."
  },
  {
    "type": "single",
    "question": "Part 1 of 5: Which line should replace `____` to create the cursor used by the following `execute()` call?\n\nThis is a progressive code set: each part builds on the same program and later parts reveal the completed code. Answer the parts in order.\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\n____\nc.execute('SELECT * FROM tasks')\nrow = c.fetchone()\nprint(row)\n```",
    "options": [
      "`c = sqlite3.cursor(conn)`",
      "`c = conn.cursor()`",
      "`c = conn.execute()`",
      "`c = cursor(conn)`"
    ],
    "answer": [
      1
    ],
    "explanation": "Incorrect - the correct answer is B: `c = conn.cursor()`.\n\n`sqlite3.connect()` returns a `Connection` object. Its `cursor()` method creates a `Cursor`, and the cursor is the object that executes the `SELECT` statement and retrieves its rows.\n\nThe completed code is:\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\nc.execute('SELECT * FROM tasks')\nrow = c.fetchone()\nprint(row)\n```\n\nExecution trace:\n\n| Step | Operation | Result |\n|------|-----------|--------|\n| 1 | `sqlite3.connect('todo.db')` | `conn` refers to a `sqlite3.Connection`. |\n| 2 | `conn.cursor()` | A `sqlite3.Cursor` is created. |\n| 3 | Assignment to `c` | `c` refers to that cursor. |\n| 4 | `c.execute(...)` | The cursor executes the query and holds its result set. |\n\nOption A is wrong because `cursor()` is not a module-level `sqlite3` function. Option C calls `execute()` without the required SQL statement and does not create a cursor. Option D refers to an undefined name. The official API documents `Connection.cursor()` as the method that creates and returns a `Cursor`. Source: [Python sqlite3 documentation](https://docs.python.org/3/library/sqlite3.html#sqlite3.Connection.cursor)."
  },
  {
    "type": "single",
    "question": "Part 2 of 5: What is the type of `c` immediately after the `execute()` call?\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\nresult = c.execute('SELECT * FROM tasks')\n\nprint(type(c).__name__)\nprint(result is c)\n```",
    "options": [
      "`list`, and `False`",
      "`tuple`, and `False`",
      "`sqlite3.Cursor`, and `True`",
      "`sqlite3.Connection`, and `True`"
    ],
    "answer": [
      2
    ],
    "explanation": "Incorrect - the correct answer is C: `c` is a `sqlite3.Cursor`, and `result is c` is `True`.\n\n`conn.cursor()` creates the cursor. Calling `Cursor.execute()` executes the SQL and returns the same cursor object, so assigning the call to `result` creates another name for that object. It does not fetch rows and does not create a list.\n\nThe output is:\n\n```text\nsqlite3.Cursor\nTrue\n```\n\nStep by step:\n\n1. `conn.cursor()` creates a `sqlite3.Cursor`, so `c` has that type.\n2. `c.execute('SELECT * FROM tasks')` runs the query.\n3. `execute()` returns `c` itself.\n4. `result is c` therefore evaluates to `True`.\n\nOption A is wrong because no fetch method has been called, and a cursor is not a list. Option B confuses one fetched row with the cursor. Option D confuses the connection that owns the cursor with the cursor itself. Source: [Python sqlite3 Cursor.execute](https://docs.python.org/3/library/sqlite3.html#sqlite3.Cursor.execute)."
  },
  {
    "type": "single",
    "question": "Part 3 of 5: Assume `tasks` contains these rows in query order. What are the type and value of `row` after the first `fetchone()`?\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\nc.execute('SELECT * FROM tasks')\nrow = c.fetchone()\nprint(type(row).__name__)\nprint(row)\n```\n\nQuery result:\n\n```text\n(1, 'Write notes', 2)\n(2, 'Review code', 1)\n```",
    "options": [
      "`tuple`, with value `(1, 'Write notes', 2)`",
      "`list`, with value `[(1, 'Write notes', 2), (2, 'Review code', 1)]`",
      "`sqlite3.Cursor`, with value `<sqlite3.Cursor>`",
      "`str`, with value `'Write notes'`"
    ],
    "answer": [
      0
    ],
    "explanation": "Incorrect - the correct answer is A: `row` is the tuple `(1, 'Write notes', 2)`.\n\nWith the default `row_factory`, `Cursor.fetchone()` retrieves exactly one next row and returns it as a tuple. It does not return the whole result set and it does not select only one column.\n\nThe first call proceeds as follows:\n\n| Step | Cursor state or operation | Result |\n|------|---------------------------|--------|\n| 1 | `execute('SELECT * FROM tasks')` | Two rows are available in query order. |\n| 2 | First `fetchone()` | Consumes the first row. |\n| 3 | Assignment to `row` | `row == (1, 'Write notes', 2)`. |\n| 4 | `type(row).__name__` | Prints `tuple`. |\n\nThe output is:\n\n```text\ntuple\n(1, 'Write notes', 2)\n```\n\nOption B describes the list returned by `fetchall()`, not one row from `fetchone()`. Option C is the cursor before fetching. Option D would be only the second field, but `fetchone()` returns the complete row tuple. Source: [Python sqlite3 Cursor.fetchone](https://docs.python.org/3/library/sqlite3.html#sqlite3.Cursor.fetchone)."
  },
  {
    "type": "single",
    "question": "Part 4 of 5: What does the second call to `fetchone()` assign to `row`?\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\nc.execute('SELECT * FROM tasks')\nrow = c.fetchone()\nprint(row)\nrow = c.fetchone()\nprint(row)\nconn.close()\n```\n\nAssume the query returns:\n\n```text\n(1, 'Write notes', 2)\n(2, 'Review code', 1)\n```",
    "options": [
      "The first row again: `(1, 'Write notes', 2)`",
      "All rows as a list: `[(1, 'Write notes', 2), (2, 'Review code', 1)]`",
      "Only the second column: `'Review code'`",
      "The next row as a tuple: `(2, 'Review code', 1)`"
    ],
    "answer": [
      3
    ],
    "explanation": "Incorrect - the correct answer is D: the second call returns `(2, 'Review code', 1)`.\n\n`fetchone()` maintains the cursor's position in the result set. Each successful call consumes and returns the next available row.\n\nStep-by-step trace:\n\n| Call | Row available before the call | Value returned |\n|------|-------------------------------|----------------|\n| First `c.fetchone()` | `(1, 'Write notes', 2)` | `(1, 'Write notes', 2)`; the cursor advances. |\n| Second `c.fetchone()` | `(2, 'Review code', 1)` | `(2, 'Review code', 1)`; the cursor advances again. |\n\nTherefore the two `print(row)` calls produce:\n\n```text\n(1, 'Write notes', 2)\n(2, 'Review code', 1)\n```\n\nOption A would be true only if the query were executed again or the cursor were repositioned. Option B describes `fetchall()`. Option C incorrectly treats a row fetch as a column fetch; each result is the complete row tuple. Source: [Python sqlite3 Cursor.fetchone](https://docs.python.org/3/library/sqlite3.html#sqlite3.Cursor.fetchone)."
  },
  {
    "type": "single",
    "question": "Part 5 of 5: If the query has only the two rows shown below, what is the type and value of `row` after the third `fetchone()`?\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect('todo.db')\nc = conn.cursor()\nc.execute('SELECT * FROM tasks')\nfirst = c.fetchone()\nsecond = c.fetchone()\nrow = c.fetchone()\nprint(type(row).__name__)\nprint(row)\nconn.close()\n```\n\nQuery result:\n\n```text\n(1, 'Write notes', 2)\n(2, 'Review code', 1)\n```",
    "options": [
      "`tuple`, with value `()`",
      "`NoneType`, with value `None`",
      "`list`, with value `[]`",
      "`StopIteration` is raised before `print()` runs"
    ],
    "answer": [
      1
    ],
    "explanation": "Incorrect - the correct answer is B: `row` is `None`, so its type name is `NoneType`.\n\n`fetchone()` returns the next row as a tuple while a row remains. Once all rows have been consumed, it returns `None` instead of raising `StopIteration`. This means code can test for the end with `if row is None:`.\n\nExecution trace:\n\n| Call | Return value | Type |\n|------|--------------|------|\n| First `fetchone()` | `(1, 'Write notes', 2)` | `tuple` |\n| Second `fetchone()` | `(2, 'Review code', 1)` | `tuple` |\n| Third `fetchone()` | `None` | `NoneType` |\n\nThe final output is:\n\n```text\nNoneType\nNone\n```\n\nOption A is wrong because an empty tuple is a real tuple value, not the documented end-of-results marker. Option C describes an empty result from `fetchall()`, not `fetchone()`. Option D confuses cursor fetching with direct iterator calls; `fetchone()` signals no remaining row by returning `None`. Source: [Python sqlite3 Cursor.fetchone](https://docs.python.org/3/library/sqlite3.html#sqlite3.Cursor.fetchone)."
  }
]
